FIND_PACKAGE(Caffe REQUIRED)
FIND_PACKAGE(OpenCL REQUIRED)

# Build our NUClear module
NUCLEAR_MODULE(INCLUDES ${OpenCL_INCLUDE_DIRS} ${Caffe_INCLUDE_DIRS}
               LIBRARIES ${Caffe_LIBRARIES} ${OpenCL_LIBRARIES})
