# Build our NUClear module
FIND_PACKAGE(Boost REQUIRED)
FIND_PACKAGE(OpenCL REQUIRED)
FIND_PACKAGE(Caffe REQUIRED)
NUCLEAR_MODULE(INCLUDES ${Boost_INCLUDE_DIRS} ${OpenCL_INCLUDE_DIRS} ${Caffe_INCLUDE_DIRS} LIBRARIES ${Boost_LIBRARIES} ${OpenCL_LIBRARIES} ${Caffe_LIBRARIES})
