FIND_PACKAGE(OpenCL REQUIRED)
FIND_PACKAGE(PythonInterp 3 REQUIRED)

# Build our NUClear module
NUCLEAR_MODULE(INCLUDES ${OpenCL_INCLUDE_DIR} "${CMAKE_CURRENT_BINARY_DIR}/src/mesh"
               LIBRARIES ${OpenCL_LIBRARY})
