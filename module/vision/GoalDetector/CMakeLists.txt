FIND_PACKAGE(Boost REQUIRED COMPONENTS serialization)
FIND_PACKAGE(OpenCV2 REQUIRED)

# Build our NUClear module
NUCLEAR_MODULE(INCLUDES ${BOOST_INCLUDE_DIR} ${EIGEN_INCLUDE_DIR} ${OPENCV2_INCLUDE_DIR}
LIBRARIES ${Boost_LIBRARIES} ${Eigen_DENSE_LIBRARY} ${OpenCV2_LIBRARIES})
